# Agent instructions

Read `CLAUDE.md`. It binds every human and every AI coding agent in this repo, whatever tool you are.
The short version: run `make check` before every push, never edit `packages/core` without a message in the team
chat first, and never write a secret anywhere but `.env`.
