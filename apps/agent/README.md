# trip-agent

Status: **owner Lane A (Wai Kin)**. The loop (`loop.py`), which implementation backs each Protocol (`registry.py`),
the CLI (`cli.py`, `uv run trip`), the Streamlit page (`ui.py`, `make ui`) and the JSONL call log (`log.py`).
Only Lane A edits `ui.py`.
