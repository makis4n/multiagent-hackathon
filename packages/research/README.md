# trip-research

Status: **unclaimed**. Claim it by putting your name on this line in your first commit.

Lane B. Reddit, YouTube and web search to typed `Signal`s. Implement `build_sources()` in
`trip_research/__init__.py`; `REAL_RESEARCH=1` in `.env` selects it. Each source is a class with a `name` and
`search(brief) -> list[Signal]`. Record one real response per source as a test cassette; tests run offline.
