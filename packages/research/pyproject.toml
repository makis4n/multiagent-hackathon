[project]
name = "trip-research"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["trip-core", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["trip_research"]
