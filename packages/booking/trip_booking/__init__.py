"""Lane D. Flights, stays and activities behind one BookingProvider."""

from trip_core.tools import BookingProvider


def build_provider() -> BookingProvider:
    """Duffel test mode behind it. Selected by REAL_BOOKING=1."""
    raise NotImplementedError("Lane D: implement trip_booking.build_provider()")
